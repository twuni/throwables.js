import Throwable from '../throwable';

/**
 * A type of error that indicates an operation succeeded, but that the results may not be as intended.
 */
class Panic extends Throwable {
  // Nothing to see here. Move along, now.
}

export default Panic;

